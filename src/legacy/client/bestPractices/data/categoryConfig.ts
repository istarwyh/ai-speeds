// Re-export types for external use
export type { CategoryConfig } from '../types/PracticeCard';

export const categoryIcons: Record<string, string> = {
  workflow: '🔄',
  configuration: '⚙️',
  'mcp-commands': '🛠️',
  context: '📝',
  automation: '🤖',
  concurrency: '👥',
  tools: '🔧',
};
